import math, numpy;

def rotate(poly_pos, cam_look):
    #get angle between cam_look and [0,0,1]
    origin_look = numpy.matrix([[0],[0],[1]]);
    
    #pitch_a = 
    yaw_a = 
    #roll_a = #need cam up?
    
    print(yaw_a);
    #rotation matrix * poly_vec
    #return ;
    
rotate([1,0,3], numpy.matrix([[1],[0],[0]]) );
